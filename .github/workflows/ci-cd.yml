name: CI CD

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npm test
      - run: npx playwright install --with-deps chromium
      - name: Start Worker locally
        run: |
          npm run dev > /tmp/worker.log 2>&1 &
          npx wait-on http://127.0.0.1:8787/api/health
      - run: npm run test:e2e
        env:
          WORKER_BASE_URL: http://127.0.0.1:8787
      - run: npm run ai:review
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 14

  deploy:
    needs: test
    runs-on: ubuntu-latest
    environment: production
    if: ${{ github.ref == 'refs/heads/main' && secrets.CLOUDFLARE_API_TOKEN != '' && secrets.CLOUDFLARE_ACCOUNT_ID != '' }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy
      - name: Production health check
        if: ${{ secrets.WORKER_BASE_URL != '' }}
        run: curl --fail --silent --show-error "$WORKER_BASE_URL/api/health"
        env:
          WORKER_BASE_URL: ${{ secrets.WORKER_BASE_URL }}
